import 'package:flutter/material.dart';

class ItemBookDetail extends StatelessWidget {
  const ItemBookDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
